// import db from "@/utils/db";

function AboutPage() {
  //// JUST SAVING STUFF
  // try {
  //   const allData = await db.testProfile.findMany();
  //   console.log(allData);
  //   return (
  //     <div>
  //       {allData.map((item) => {
  //         return <h1 key={item.id}>{item.name} , {item.id}</h1>;
  //       })}
  //     </div>
  //   );
  // } catch (error) {
  //   console.error("Database error:", error);
  //   return <div>Error loading data. Check console for details.</div>;
  // }

  return <div>Hello world </div>;
}

export default AboutPage;
